<r-fit><h1>{{ title }}</h1></r-fit>
{{ for row of rows }}
<r-fit>
  <c-50>
    <h2>{{ row.cols[0].title }}</h2>
    <p>{{ row.cols[0].text }}</p>
  </c-50>
  <c-50>
    <h2>{{ row.cols[1].title }}</h2>
    <p>{{ row.cols[1].text }}</p>
  </c-50>
</r-fit>
{{ /for }}
